const NotFound = () => (
  <section className="page narrow">
    <div className="card">
      <div className="card-title">Page not found</div>
      <p className="muted">Try heading back to the search page.</p>
    </div>
  </section>
);

export default NotFound;
